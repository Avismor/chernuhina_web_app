Не успела проделать это задание из-за того, что "веб-приложение" не было перенесено и настроено на машине, но распишу как бы оно выполнялось (вдруг зачтётся хоть сколько-то)

Для регистрации пользователя с логином в формате Фамилия_ИО, используя только команду curl (без браузера и без готовых скриптов), нужно отправить POST-запрос на скрипт регистрации back/register.php.
Пример cURL:

curl -X POST http://localhost:8080/back/register.php \
     -d "login=ivanov_ii" \
     -d "password=пароль"

